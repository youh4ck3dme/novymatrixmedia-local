rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Production-safe default: deny all until explicit paths are approved.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
