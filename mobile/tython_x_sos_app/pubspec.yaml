name: tython_x_sos_app
description: Tython X SOS Light cross-platform starter app.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  connectivity_plus: ^6.0.5
  flutter_secure_storage: ^9.2.2
  geolocator: ^12.0.0
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  firebase_app_check: ^0.4.1+7
  firebase_core: ^4.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
