rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Production-safe default: deny all until explicit collections are modeled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
